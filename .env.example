# GraphQL API 端点配置

# 本地开发时使用（可选，默认会使用 Vite 代理）
# VITE_GRAPHQL_ENDPOINT=http://localhost:8787/graphql

# 生产环境：部署的 Workers URL
# VITE_WORKERS_URL=https://daily-agent-workers.your-subdomain.workers.dev
